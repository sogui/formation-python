# 03 — Fonctions

**Objectifs** : définir des fonctions propres, documentées, testables.

## Déroulé

1. `def`, paramètres, `return`
2. Arguments nommés et valeurs par défaut
3. Portée des variables (locale / globale)
4. Docstrings et annotations de types
5. Petites vérifications avec `assert`

## TP

Complétez `tp_penalites.py` : le calcul des pénalités de retard, découpé en
fonctions réutilisables.
